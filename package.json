{
  "name": "rekan-vault-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm --recursive run build",
    "lint": "pnpm --recursive run lint",
    "test": "pnpm --recursive run test",
    "typecheck": "pnpm --recursive run typecheck",
    "export-schemas": "python -m rekanvault.contracts.export"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
